import { SpecFieldDirective } from './spec-field.directive';

describe('SpecFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecFieldDirective();
    expect(directive).toBeTruthy();
  });
});
